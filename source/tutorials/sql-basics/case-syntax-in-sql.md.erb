---
title: Using the CASE syntax in SQL queries
description: |
  Basically, it's like the IFELSE() function in spreadsheets.
takeaways: 
  - How to write a `if x, then y` statement, but using `CASE` and `WHEN`
  - How to write a `if x, then y, else z` statement, but using `CASE`, `WHEN`, and `ELSE`
  - The difference between a statement in the form of `CASE WHEN x = 1...` and a statement in the form of `CASE x = 1 WHEN...`
listed: true
references:
  - title: The CASE expression
    source: SQLite documentation
    url: http://www.sqlite.org/lang_expr.html#between
---


